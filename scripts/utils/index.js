const utils_checks = require('./checks');
const utils_date = require('./date');
const utils_files = require('./files');
const utils_generic = require('./generic');
const utils_path = require('./path');

module.exports = {
    checks: utils_checks,
    date: utils_date,
    files: utils_files,
    generic: utils_generic,
    path: utils_path
};